# Copy to .env.local and adjust if needed.
# In dev, Vite proxies /api to the backend (see vite.config.ts), so you can
# leave this unset. Set it only when proxying isn't possible (e.g. deploying
# the frontend separately from the backend).
VITE_API_URL=http://localhost:5000/api
